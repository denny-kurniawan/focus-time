import { StyleSheet, Text, View } from 'react-native';

type Props = {};

const Focus: React.FC<Props> = (props) => {
  return (
    <View>
      <Text>Focus Features</Text>
    </View>
  );
};

export default Focus;

const styles = StyleSheet.create({
  container: {},
});
